<Page x:Class="D6_ex.FluxPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:D6_ex.ViewModels"
      xmlns:yb="clr-namespace:YControls.FontIconButtons;assembly=YControls"
      DataContext="{Binding Source={x:Static vm:FluxPageViewModel.Instance}}"
      xmlns:local="clr-namespace:D6_ex"
      mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
       
        <Button Grid.Column="1"
                Click="Button_Click"
                Background="White">Two</Button>
        <Button Grid.Column="2"
                Background="White">Three</Button>
    </Grid>
</Page>
