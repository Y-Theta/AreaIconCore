<yt:YT_Window x:Class="AreaIconCore.MainWindow"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:yt="clr-namespace:YControls;assembly=YControls"
              xmlns:service="clr-namespace:AreaIconCore.Services"
              xmlns:local="clr-namespace:AreaIconCore"
              DragingMode="FullWindow"
              Background="Transparent"
              ExtendToTitleBar="True"
              mc:Ignorable="d"
              Title="MainWindow"
              Height="400"
              Width="640">
    <Grid Background="#80000000">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <ListView ItemsSource="{Binding Extensions,Source={x:Static service:CoreSettings.Instence}}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding DataContext,RelativeSource={RelativeSource Mode=Self}, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Click="Button_Click">Save</Button>
            <Button Click="Button_Click_1"
                    Grid.Column="1">Show</Button>
        </Grid>
    </Grid>
</yt:YT_Window>
